APP_NAME=BNR Approval System API
ENVIRONMENT=development
DEBUG=true

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=bnr_user
DB_PASSWORD=change_me
DB_NAME=bnr_approval

JWT_SECRET=change_to_a_long_random_value
JWT_ALGORITHM=HS256
JWT_EXPIRES_MINUTES=1440

# Comma-separated list (for local + deployed frontend)
CORS_ORIGINS=http://127.0.0.1:5500,http://localhost:5500,https://your-frontend-site.netlify.app
